<h1>Listing admissions</h1>

<table>
  <tr>
    <th>Product</th>
    <th>Totalprice</th>
    <th>Start date</th>
    <th>End date</th>
    <th>Ward type</th>
  </tr>

<% @admissions.each do |admission| %>
  <tr>
    <td><%=h admission.product_id %></td>
    <td><%=h admission.totalprice %></td>
    <td><%=h admission.start_date %></td>
    <td><%=h admission.end_date %></td>
    <td><%=h admission.ward_type %></td>
    <td><%= link_to 'Show', admission %></td>
    <td><%= link_to 'Edit', edit_admission_path(admission) %></td>
    <td><%= link_to 'Destroy', admission, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New admission', new_admission_path %>